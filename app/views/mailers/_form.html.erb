<%= form_with(model: mailer) do |form| %>
  <% if mailer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mailer.errors.count, "error") %> prohibited this mailer from being saved:</h2>

      <ul>
        <% mailer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :UserMailer, style: "display: block" %>
    <%= form.text_field :UserMailer %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
